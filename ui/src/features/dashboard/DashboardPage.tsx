// import { useAuth } from "../../hooks/useAuth.tsx";

function DashboardPage() {
  // const { isAuthenticated } = useAuth();
  return (
    <>
      <h2>Dashboard</h2>
      {/*<p>{isAuthenticated ? "Authed" : "notauthed"}</p>*/}
    </>
  )
}

export default DashboardPage;
